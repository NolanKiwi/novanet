[package]
name = "novanet-wire"
description = "Binary packet encoder and decoder for the NovaNet protocol"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
novanet-core.workspace = true
bytes.workspace = true
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true
criterion.workspace = true

[[bench]]
name = "packet_codec"
harness = false
