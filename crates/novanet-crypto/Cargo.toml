[package]
name = "novanet-crypto"
description = "Cryptographic primitives for NovaNet: key exchange, AEAD, signing (Phase 4)"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
novanet-core.workspace = true
thiserror.workspace = true
