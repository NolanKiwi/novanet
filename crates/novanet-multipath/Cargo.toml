[package]
name = "novanet-multipath"
description = "Path tracking, validation, and migration for NovaNet (Phase 6)"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
novanet-core.workspace = true
