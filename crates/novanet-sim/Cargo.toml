[package]
name = "novanet-sim"
description = "Packet loss, delay, and jitter simulation utilities for NovaNet testing"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
novanet-core.workspace = true
rand.workspace = true
